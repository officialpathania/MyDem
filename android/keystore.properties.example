# Copy to keystore.properties (same folder as this file: android/) and fill in real values.
# keystore.properties is gitignored — never commit it.

storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=mydem-release
storeFile=keystore/mydem-release.keystore
